// Este arquivo simula um painel de configurações de um CMS.

export const siteSettings = {
  // Para ativar o banner da Black Friday na página inicial, mude para `true`.
  // Para desativar, mude para `false`.
  isBlackFridayActive: false,
};
